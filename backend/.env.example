# Server
PORT=3000

# Super Admin login password
MASTER_PASSWORD=change_me

# Firebase — for APK push notifications
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Supabase PostgreSQL (URL-encode special chars in password: @ → %40, $ → %24, # → %23)
DATABASE_URL=
